<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <ImageView
        android:id="@+id/reminderOverviewThumbnail"
        android:layout_width="75px"
        android:layout_height="75px"
        android:scaleType="centerCrop"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription=""
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/reminderOverviewDescription"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/reminderOverviewThumbnail"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Reminder: 10:00 10.10.2010"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/reminderOverviewPicture"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/reminderOverviewDescription"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/reminderOverviewThumbnail"
        android:gravity="center_vertical"
        android:text="Picture 1"
        android:textSize="16sp" />

</RelativeLayout> 